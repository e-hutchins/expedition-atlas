// expeditions/empire-builder/js/main.js
//
// Entry point for the Empire Builder expedition page.

/**
 * Initialize the Empire Builder map: create the Leaflet map instance,
 * load each GeoJSON layer (routes, stations, POIs) via
 * shared/js/geojson-layer.js, and wire up the layer control and legend.
 *
 * Implementation is intentionally deferred to a later pass -- this is
 * the documented entry point that pass will fill in.
 *
 * @returns {void}
 */
function initEmpireBuilderMap() {
  // Not implemented yet -- map creation and layer loading come in a
  // later pass.
}

document.addEventListener("DOMContentLoaded", initEmpireBuilderMap);
